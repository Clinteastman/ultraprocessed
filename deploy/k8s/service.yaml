apiVersion: v1
kind: Service
metadata:
  name: ultraprocessed-backend
  namespace: ultraprocessed
  labels:
    app.kubernetes.io/name: ultraprocessed
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ultraprocessed
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
