# Vite (and a few other tools) reach for transitive deps that aren't
# declared in their own package.json. pnpm's strict isolation breaks them.
# Hoisting matches npm/yarn behaviour and fixes the build inside Docker.
shamefully-hoist=true
